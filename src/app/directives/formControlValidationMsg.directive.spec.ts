/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FormControlValidationMsgDirective } from './formControlValidationMsg.directive';

describe('Directive: FormControlValidationMsg', () => {
  it('should create an instance', () => {
    const directive = new FormControlValidationMsgDirective();
    expect(directive).toBeTruthy();
  });
});
